# Code and analysis review — 14 September 2026

Scope: every script in `analysis/`, every exhibit and table in `outputs/`, `outputs/RESULTS.md`,
checked against `prompt.pdf`, `PLAN.md` and `CLAUDE.md`. All numbers below are from the
regenerated `outputs/RESULTS.md` and the tables it cites. The pre-review log is archived at
`build/_old/RESULTS_before_review_2026-09-14.md`.

## 1. What was wrong and is now fixed

| # | Problem | Where | Effect on the story | Fix |
|---|---------|-------|---------------------|-----|
| 1 | **Wrong hypothesis in the wild-cluster bootstrap.** It tested "the essay slope in new-ATS/AI-era hires is zero", which is exactly what the data show, so p = 0.49 was inevitable. RESULTS.md then reported this as "the hole". | `07_identification.py` | The narrative would have said the headline result does not survive small-cluster inference. The correct test (slope difference, legacy pre-2023 vs new post-2023) gives p < 0.01 on both hires and flex. | Explicit slope-difference columns; restricted model imposes the right null. E15d. |
| 2 | **Three regimes hid the legacy decline.** "Legacy" pooled 2021-25. The timed system's essay slope also falls after 2023 (hires 0.0087 to 0.0048; flex −0.179 to −0.122, E12b/E12d). Legacy essays also got faster (sub-10-minute share 6% to 12%, E07b). | `utils.py` and every slope script | The old story ("timed section still works") overstated option (b): a timed field slows the leak, it does not stop it. The seven legacy centres are an imperfect proxy for a timed section. | Four ATS x era cells (`regime4`) for every slope comparison. Signal retained: new ATS AI-era [0%, 39%], legacy AI-era [55%, 68%] (E15e). |
| 3 | **"DiD" numbers were the post-era gap, not the interaction.** The `treated:C(post_ai) + C(post_ai)` form estimates two level effects; RESULTS.md reported the second as "the DiD". The post-AI dummy is also collinear with month FE, which made the cluster covariance numerically unstable (flex DiD standard error of 74 on a reopen rate; NaN standard errors on the essay regression). | `02`, `03`, `04` | Retention is barely affected (−0.022 vs −0.021). Reopen changes: the DiD interaction is 0.13 [−0.05, 0.30], not distinguishable from zero; only the AI-era gap (0.24 [0.06, 0.42]) is. Essay inflation: 0.79 points DiD on top of a 0.89-point pre-AI new-ATS effect (total 1.68). | `DID_TERMS` + `did_table()` in utils: reports pre-era effect, interaction, and their sum with a proper SE. E01b, E02b, E03c, E06c, E08b, E09b, E10d. |
| 4 | **Timing (H7) model had no main effects**, so the essay-by-speed slopes absorbed level differences. | `06` | The corrected result is stronger: fast essays on the new ATS in the AI era carry a negative slope (−0.016 [−0.021, −0.012]); slow essays still work everywhere (E13b). | `C(regime4)*C(fast15)` main effects added. |
| 5 | **Annual dollar figures multiplied a per-retained-employee cost by hires**, not retained seats (about 20% overstatement). | `08` | Annual saving for (b) now $7.7M-$10.6M excl. quality. | Multiply by hires x r. |
| 6 | **Status quo cohort mismatched the counterfactual window** (2023-25 vs legacy 2024-25). 2023 was a transition year with r = 0.87. | `08` | Status quo r is 0.819, not 0.834; the gap to legacy is 3.7 points, not 2.3. Both are now shown (E17 rows 1-2) and the DiD-implied gain (2.2 points) is the low bound. | Status quo = new-ATS hires starting 2024-01 or later. |
| 7 | **Option (b) was costed at the legacy centres' shorter days to fill**, a free $550 the prompt tells us not to assume. | `08` | (b) now costed at status-quo days to fill; legacy-observed is the "high" row; E20 varies it. | E19 rows b-low / b / b-high, E20c break-evens. |
| 8 | Option (d) label said "one third of the gap", code used one sixth. | `08` | Label now derived from the constant. | |
| 9 | Hard-coded numbers in the H7 log text; `r` rounded to two decimals in E17/E19 tables; E17b referenced but never written; recruiter screening cost computed but unreported. | `06`, `08` | Cosmetic but violated CLAUDE.md. | All from data; E17a/E17b written. |
| 10 | E06 title said legacy scores are flat (they drift 17.2 to 18.1); E06 lacked migration-wave markers; default matplotlib colour cycling; E10c dumped 100 fixed-effect rows. | `03`, `04` | | Fixed titles, markers, Okabe-Ito palette assigned by entity, FE rows dropped. |
| 11 | `log_result` appended on every run, so RESULTS.md accumulated duplicates. | `utils.py` | | Now replaces the block for the same hypothesis. |

Verified and unchanged: `starter.load()` joins (E00 all match), ATS at application month
throughout, censored hires dropped (41,677 of 46,266), the worksheet unit test (C_hire $18,942;
$26,103; $11,800), 7 never-migrated centres, the 2023-01 cutoff (grid search shows a ramp, not a
jump, so any 2023 cutoff tells the same story, E06b).

### 1b. `/code-review analysis/` (run after the fixes above) — seven findings, all verified and fixed

| # | Finding | Effect | Fix |
|---|---------|--------|-----|
| 12 | Annual saving range for (b) in the H11 log used the central row as its low end, not the DiD-only row. | Low end was $7.7M; correct is $4.9M (excl. quality). | E19 row index corrected. |
| 13 | Hire-level DiD dropped the post-AI dummy on the premise that start-month FE absorb it; they do not, because post-AI is defined on application month (594 hires straddle the cutoff). | Retention DiD −0.0216 → −0.0232 (95% CI [−0.042, −0.004]); reason-specific DiDs now all include zero, so the "voluntary exits rise" sentence was removed. | `DID_TERMS_HIRES` includes post_ai for any spec with start-month FE. |
| 14 | AI-era cutoff hard-coded in figure annotations and labels in 02, 03, 05, 07. | Exhibits would silently disagree with regressions if the cutoff moved. | All derived from `AI_ERA_CUTOFF`. |
| 15 | Literal numbers from an earlier run in two log strings. | | Computed from data. |
| 16 | Bootstrap rebuilt the design matrix 299 times per call. | 07 took ~20 min. | Built once with patsy; 07 now ~5 min; identical p-values (same seed). |
| 17 | Fast-essay flag redefined locally instead of using the utils definition. | | `fast_essay` / `fast_essay15` from `add_regime`. |
| 18 | Event-window construction, control lists and essay bins duplicated between 06 and 07. | | Moved to utils (`event_window`, `HIRE_CTRL`, `FLEX_CTRL`, `ESSAY_BINS`). |

The log is now sorted by hypothesis number on every write.

## 2. Gaps still open

1. **Narrative not started.** `report/narrative_outline.md` now exists; no prose yet.
2. **Option (c) and (d) retention effects are assumptions**, not estimates. There is nothing in
   the data to estimate them from; the report must say so and lean on E20c (option (c) must
   recover 89% of the legacy gap at $204 per hire and +3 days to break even).
3. **Implementation cost of (b) is not in the data.** Show break-even: the annual saving in E19
   is the ceiling a vendor change can cost.
4. **Legacy centres as a proxy for (b)**: they are small (headcount 113-239) and southern/mid-
   Atlantic. A size-and-region matched comparison was not done. This is the first Round 2 risk.
5. **Legacy hires' own erosion is borderline** on hires (bootstrap p = 0.08) and clear on flex
   (p = 0.003). Report both.
6. Not done from PLAN.md, and I recommend leaving them out (they do not serve the three
   answers): count-model robustness for reopens; ±6-month cutoff sensitivity table (E06b covers
   it); a "recruiter minutes buy retention" regression (a quick check gave slopes
   indistinguishable from zero in every cell, so nothing to report); coaching-hours regression
   (positive, 0.006 per hour, but coaching hours are recorded after hiring and are endogenous to
   who stays, so it cannot support option (d)).
7. `/code-review` has been run and its findings fixed (section 1b); no open code findings.

## 3. Plan to finish

1. Read every exhibit PNG once for label collisions (E12 and E13 subtitles are long).
2. Draft the four pages with `/case-writing`, in this order: recommendation block, section (b),
   section (a), section (c). Copy every number from RESULTS.md with its exhibit id.
3. Build the appendix in the order listed at the end of `narrative_outline.md`; each figure
   gets its table underneath.
4. One teammate reads the draft cold and, for every number, opens the cited exhibit. Any number
   that cannot be found is deleted or fixed.
5. Spell-check; consistent spelling (American, since the case uses "center").
6. Round 2 prep sheet: the five triggers in H12, plus what packet content would flip each.

## 4. Which graphs answer the questions, and what matters most

**Lead exhibit (put it on page 1 next to the recommendation): E12.** Four panels, retention by
essay bin. In three cells a 24-30 essay retains better than an 18-23 essay; in the new-ATS
AI-era cell it retains worse (0.829 vs 0.853). One picture, no regression needed to read it.

**Deliverable (a), the process.**
- E11 (coefficient plot): the essay is the only input whose slope collapses; license,
  referral, resume and interview hold or strengthen. This is the "gets right / fails" judgment
  in one chart.
- E10 (four-panel capacity): applications per opening up 45%, minutes per application down
  53%, days to fill up 7 days. The failure is capacity as well as signal.
- E01 (retention by quarter, two lines) for the trend Whitfield sees; E04 (by migration wave)
  to explain "worst in our best-run centers".
- Table E11c in the appendix: the screen score is a fixed formula (weight 0.250 in every
  cell). Say it in one sentence in the text.

**Deliverable (b), the essay.**
- E12 (above) plus E13: essays written in 15+ minutes still predict; fast high scores on the
  new ATS now predict worse retention. This is the mechanism and it points straight at the fix.
- E06 (monthly essay mean, two lines, wave markers): the rise starts in 2023 in all new-ATS
  centers regardless of migration date, which is what separates "AI era" from "new software".
- E14 as a table, one line in the text: the placebo inputs did not move.
- E15 (event study) and E12e/E15d as appendix tables for "what must be true".
- State the range in words: the new ATS keeps 0-39% of the essay's signal, the timed one
  55-68%, and say why it is a range (hires are selected on the essay, flex are not).

**Deliverable (c), the decision.**
- E19 (table, not a chart): the four options against the status quo, with the (b) low/high
  rows visible so the range is on the page.
- E20 (sensitivity lines): the one chart for section (c). It shows (b) beats the status quo at
  every retention bound with no extra vacancy days, and where it stops (about a week).
- E18 (separation timing) in one sentence: 47% of leavers are gone by month two, which is why
  "sort them after hiring" buys little.

**Most important things to get right in the text.**
1. The recommendation is (b), and its retention effect is the only one that is observed, not
   assumed. Cost range per retained employee: $665-$1,556 cheaper excluding quality,
   $2,501-$3,392 including it; $4.9M-$10.6M a year excluding quality (E19).
2. Name the hole up front: the legacy centers are small and their own essay signal is eroding;
   a timed field is a partial fix, and the true gain lies between the DiD bound and the
   legacy-observed bound.
3. Flag the fast-essay signal as the cheap add-on to (b): Meridian already records minutes in
   section and does not use them (E13).
4. Do not claim the reopen DiD is significant; the AI-era gap is, the interaction is not
   (E02b). Report both.
5. Say what could not be determined: individual AI use, the 94% unreviewed, implementation
   cost, applicant drop-off under a timed section.
